---
title: "MCP Examples"
description: "End-to-end workflows for common Flowstep MCP tasks."
sidebarTitle: "Examples"
---

## 1. Generate and preview a screen

The most common workflow: create a file, generate a screen, view the result.

```
1. create-file      { "title": "Auth screens" }
                    → { "id": "FILE_ID" }

2. ai-prompt        { "fileId": "FILE_ID",
                      "message": "Mobile sign-in screen for a developer tool.
                                 Email/password fields, Google SSO button, dark theme." }
                    → { "answerId": "ANSWER_ID" }

3. get-screen-image { "fileId": "FILE_ID", "elementId": "ANSWER_ID" }
                    → [inline PNG]
```

**Prompt to your AI assistant:**
> Create a new Flowstep file called "Auth screens", generate a mobile sign-in screen for a developer tool with email/password and a Google SSO button in dark theme, then show me the image.

---

## 2. Import a React component

Use `import-jsx` to get a visual snapshot of an existing component without running the app.

```
import-jsx {
  "fileId": "FILE_ID",
  "elements": [
    {
      "jsxType": "div",
      "props": { "className": "flex flex-col gap-4 p-6 bg-background" },
      "children": [
        {
          "jsxType": "Card",
          "children": [
            { "jsxType": "h2", "props": { "className": "text-lg font-semibold" },
              "children": [{ "text": "API Keys" }] },
            { "jsxType": "Input", "props": { "placeholder": "sk-..." } },
            { "jsxType": "Button", "children": [{ "text": "Generate" }] }
          ]
        }
      ]
    }
  ],
  "dimensions": [1440, 1024],
  "label": "API key settings panel",
  "colorTheme": "default",
  "screenType": "desktop-medium"
}
→ { "answerId": "ANSWER_ID" }
```

Then call `get-screen-image` to verify the result.

**Prompt to your AI assistant:**
> Read `src/components/settings/ApiKeys.tsx`, translate it to a Flowstep JSX import tree, and import it into my "Settings" Flowstep file. Show me the rendered image.

---

## 3. Iterate on a MCP-generated screen

`regenerate` and `edit` require design context only present for web-app generations. For MCP-generated screens, the workaround is to render the screen as an attachment and call `ai-prompt` with the image.

```
1. upload-attachment { "fileId": "FILE_ID", "elementId": "ANSWER_ID" }
                     → { "id": "ATT_ID", "path": "...", "type": "image", "mimeType": "image/jpeg" }

2. ai-prompt         { "fileId": "FILE_ID",
                       "message": "Change the color scheme to dark mode and
                                   add a 'Remember me' checkbox below the password field.",
                       "attachments": [{ "id": "ATT_ID", "path": "...", "type": "image",
                                         "mimeType": "image/jpeg" }] }
                     → { "answerId": "NEW_ANSWER_ID" }

3. get-screen-image  { "fileId": "FILE_ID", "elementId": "NEW_ANSWER_ID" }
```

---

## 4. Expand a screen to a full flow

Add follow-on screens to an existing design.

```
1. list-screens      { "fileId": "FILE_ID" }
                     → [{ "answerId": "BASE_ANSWER_ID", "prompt": "Sign-in screen", ... }]

2. ai-prompt         { "fileId": "FILE_ID",
                       "message": "Add the next screen in the user flow",
                       "operation": "expand",
                       "operationVariant": "next_screen",
                       "targets": ["BASE_ANSWER_ID"] }
                     → { "answerId": "NEXT_ANSWER_ID" }

3. get-screen-image  { "fileId": "FILE_ID", "elementId": "NEXT_ANSWER_ID" }
```

Repeat step 2 with `operationVariant: "error_state"` or `"empty_state"` to add edge-case screens.

<Note>
  `expand` requires the base screen to have been generated from the Flowstep web app. For MCP-generated screens, use the attachment workaround from Example 3.
</Note>

---

## 5. Audit quota before a batch job

Check remaining capacity before kicking off multiple generations.

```
get-plan-details {}
→ {
    "remaining": {
      "messages": { "daily": 38, "monthly": 413 }
    }
  }
```

If `daily` is low, schedule the batch for after 00:00 UTC when it resets.
